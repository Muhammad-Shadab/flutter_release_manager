name: flutter_build_release
description: CLI tool to build Flutter APK/IPA and upload to Google Drive (via rclone) or Diawi.
version: 1.0.2
repository: https://github.com/Muhammad-Shadab/flutter_build_release

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.5.0
  http: ^1.2.0

dev_dependencies:
  lints: ^4.0.0